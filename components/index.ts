// a single file to export all our components,
// so we can import them simpler in the pages where we use them
// NOTE: I believe this is for VisualBasic, PHPStorm does this automatically

import Hero from './Hero';

export {
    Hero
};